# ローカル開発用の設定。本番は CDK 設定（非機密）と SSM SecureString（機密）で供給する。
# 非機密（本番は CDK / CloudFront 由来）
PUBLIC_ORIGIN=http://localhost:5173
GOOGLE_CLIENT_ID=
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/callback

# 機密：ローカルのみここで渡す。本番は SSM Parameter Store SecureString。
SESSION_SECRET=
GOOGLE_CLIENT_SECRET=
